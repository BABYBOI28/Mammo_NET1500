export const handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Hello world from MAmmo Yohannis Lambda!'),

    };
    return response;
};